## 1. Pick the numbers

- [x] 1.1 Draw the hierarchy row's state dot one size smaller from its call site.
- [x] 1.2 Shorten the control row, take `text-xs`, and drop `leading-none` so the icon and the word share one line box.
- [x] 1.3 Give the rail separators the chrome ground so the three columns meet without a seam.

## 2. Name the row for a person

- [x] 2.1 Treat a digits-only Pane label as the multiplexer's ordinal in the navigation model, and cover it beside the two naming cases already there.

## 3. Verify and publish

- [x] 3.1 Run the owned, tree, shell, composer and Pane tests plus both typechecks and lint.
- [x] 3.2 Add one `CHANGELOG.md` line, commit only this change's paths, push, archive, and redeploy.
