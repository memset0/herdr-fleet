## 1. Give the primitive back

- [x] 1.1 Restore Collie's sheet primitive and both actions sheets to exactly upstream's text.

## 2. Build the fork's own surfaces

- [x] 2.1 Add the context menu: portalled, measured, placed at the cursor, dismissed by a press elsewhere / Escape / the page moving, walked by the arrow keys, entering with a fade and no motion.
- [x] 2.2 Add the centred prompt, generic over what is being named so a later surface can ask its own question with it.

## 3. Choose at the invoke site

- [x] 3.1 Add one drop-in per kind of row that takes the sheet's props, renders the sheet on a device without a fine pointer or a gesture, and otherwise renders the menu with the same gates, confirms and writes.
- [x] 3.2 Point the hierarchy and both strips at the drop-in.

## 4. Verify and publish

- [x] 4.1 Cover both branches of the choice, the centred rename and the armed close, and run the sheet, actions-sheet, strip, tree and shell suites plus both typechecks, lint and the fork check.
- [x] 4.2 Replace the port in `FORK.toml`, add one `CHANGELOG.md` line, commit only this change's paths, push, archive, and redeploy.
