## 1. Put both back where they are specified

- [x] 1.1 Recompute the hierarchy's guide line and child indentation from the row's padding and the chevron's width, and state the arithmetic at the element.
- [x] 1.2 Move the Agent row's reserve for the favourite control from the button to the line that shares its corner, and pin it with a focused test.

## 2. Verify and publish

- [x] 2.1 Run the tree, shell and rail tests plus both typechecks and lint.
- [x] 2.2 Add a `CHANGELOG.md` line, commit, push, archive, and redeploy.
