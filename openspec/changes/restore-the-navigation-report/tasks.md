## 1. Put Collie's hook back

- [x] 1.1 Restore the slow-load hook's signature, its navigation signal and its test, and the root route's plain call.
- [x] 1.2 Remove the two `FORK.toml` anchors and the `CHANGELOG.md` line that recorded the withdrawn option, keeping the hierarchy's density line.

## 2. Withdraw the requirement

- [x] 2.1 Remove the route-change requirement from the canonical spec, recording the false diagnosis as its reason.

## 3. Verify and publish

- [x] 3.1 Run the hook, tree and shell tests plus both typechecks and lint.
- [x] 3.2 Commit, push, archive, and redeploy.
