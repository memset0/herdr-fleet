## 1. Take the emphasis, not just the box

- [x] 1.1 Export the dashboard's own attention set and read it from the rail rather than copying it.
- [x] 1.2 Give the rail row Collie's two densities, with the flat one's radius, hover, blocked rail and resting-ring ground.
- [x] 1.3 Group the sections the way the dashboard does — a gap list for cards, one bordered group for flat rows.

## 2. Verify and publish

- [x] 2.1 Pin the rule in the rail's own suite, and run the rail, shell and Agent-list suites plus both typechecks, lint and the fork check.
- [x] 2.2 Record the export in `FORK.toml`, add one `CHANGELOG.md` line, commit only this change's paths, push, archive, and redeploy.
