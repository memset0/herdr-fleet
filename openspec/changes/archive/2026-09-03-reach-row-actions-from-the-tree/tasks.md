## 1. What a row acts on

- [x] 1.1 Give a navigation row its subject — the Pane it stands for or the Tab it groups — and leave a Host and a Space without one.

## 2. Reaching the sheets

- [x] 2.1 Open a row's actions from Collie's own long-press hook, spread on the row so the chevron answers too, and pass the subject up.
- [x] 2.2 Resolve the subject against the current snapshot in the shell and mount Collie's two sheets with the existing read-only gate, revalidation, and return-to-dashboard rule.
- [x] 2.3 Cover a Pane row, a Tab group row, and a Space row that offers nothing.

## 3. The rail's air

- [x] 3.1 Give the rail's rows more space between them without changing their type.

## 4. Verify and publish

- [x] 4.1 Run the tree, shell and rail tests plus both typechecks, lint, the fork check and strict OpenSpec validation.
- [x] 4.2 Add a `CHANGELOG.md` line, commit, push, archive, and redeploy.
