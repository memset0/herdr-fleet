## 1. Answer where a menu goes

- [x] 1.1 Add the fork-owned placement geometry — open down and to the right, flip rather than slide at an edge, clamp only when neither side fits — and cover it.
- [x] 1.2 Add the fork-owned record of a context gesture, claimed once, briefly, and only from a mouse at a real point, and cover it.

## 2. Let the sheet stand there

- [x] 2.1 Give Collie's sheet primitive one placement port with three stands, leaving the bottom sheet byte-identical when nothing places it.
- [x] 2.2 Have each actions sheet pass the stand its gesture asked for, and the centre once its rows have become a question.
- [x] 2.3 Record the gesture once, in the shell, so all three surfaces that open those sheets inherit it.

## 3. Verify and publish

- [x] 3.1 Cover the three stands and the claim, and run the sheet, actions-sheet, strip, tree and shell suites plus both typechecks, lint and the fork check.
- [x] 3.2 Declare the port in `FORK.toml`, add one `CHANGELOG.md` line, commit only this change's paths, push, archive, and redeploy.
