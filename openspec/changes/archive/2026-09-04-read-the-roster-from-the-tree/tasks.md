## 1. Carry the machine into the row

- [x] 1.1 Give the model a Host icon kind and the member id the row stands for, and cover it.

## 2. Draw it and read it

- [x] 2.1 Put the tinted server glyph in the disclosure column of a Host row, swapping it for the refusal glyph when the machine is not answering.
- [x] 2.2 Say the state in words at the row's trailing end, so colour alone never carries it.
- [x] 2.3 Move the disclosure state onto the row's own label, which is the control that remains.

## 3. Verify and publish

- [x] 3.1 Cover a two-member roster with one member down, and run the tree, shell, rail and model suites plus both typechecks, lint and the fork check.
- [x] 3.2 Add one `CHANGELOG.md` line, commit only this change's paths, push, archive, and redeploy.
