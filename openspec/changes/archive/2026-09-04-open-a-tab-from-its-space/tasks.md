## 1. Give the Space row its one verb

- [x] 1.1 Give the Space row a subject in the model and cover it in the tree's suite.
- [x] 1.2 Add the fork-owned Space surface — the menu for a cursor, Collie's own sheet and row for a thumb — carrying only the verb the whole chain can land.
- [x] 1.3 Perform the create through Collie's own `useSpaceActions().newTab`, so the gate, the copy, the revalidation and the navigation are unchanged.

## 2. Verify and publish

- [x] 2.1 Run the model, tree, shell and row-actions suites plus both typechecks, lint and the fork check.
- [x] 2.2 Add one `CHANGELOG.md` line, commit only this change's paths, push, archive, and redeploy.
