## 1. Move the machine up

- [x] 1.1 Give the composer one default-true port that stands its host caption down, and take the band's own gate with it.
- [x] 1.2 Draw the chip in the Pane page's existing header cluster, ahead of the pane menu.

## 2. Verify and publish

- [x] 2.1 Move the existing "neither the host nor the state" assertion to the header and pin the band's absence, then run the Pane page, composer and shell suites plus both typechecks, lint and the fork check.
- [x] 2.2 Add one `CHANGELOG.md` line, commit only this change's paths, push, archive, and redeploy.
