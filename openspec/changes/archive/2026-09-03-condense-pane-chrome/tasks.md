## 1. The pane's state

- [x] 1.1 Pin Collie's own status badge to the strip row's existing trailing slot, before the fold control.
- [x] 1.2 Give the composer one prop that stands its status word down, let its band leave through `Collapse` when it would be empty, and name the dock so callers stop finding it through the band.
- [x] 1.3 Derive the switch in the Pane page from strips/folded/zen, and update the two Pane tests that found the dock through the band.

## 2. The controls and the mark

- [x] 2.1 Lay the control row's icons beside their words at one size and give the display control its own word.
- [x] 2.2 Add the mark primitive to the header's claim, decline it on the Pane route, and cover both in the header's tests.

## 3. The shell's ground and the row's state

- [x] 3.1 Move the header and both rails onto the chrome ground and pass that fill to the mark's knockout.
- [x] 3.2 Carry a Pane's state through the navigation model and draw it as the Tab row's dot at a row's trailing end.
- [x] 3.3 Bind automatic disclosure to a change of selected Pane, narrow the disclosure column and the indentation step, and sit each rail's title directly over its list.

## 4. Verify and publish

- [x] 4.1 Run the focused owned, tree, shell, header, composer and Pane tests plus both typechecks, lint, the fork check and strict OpenSpec validation.
- [x] 4.2 Add `CHANGELOG.md` lines under `Unreleased`, update `FORK.toml` for the new ports, commit only this change's paths, push, archive, and redeploy the exact pushed commit.
