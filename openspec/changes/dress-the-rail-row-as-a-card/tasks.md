## 1. Give the box back

- [x] 1.1 Wrap the rail row's contents in Collie's card treatment — border, card ground, shadow, hover and press — leaving the two lines' order alone.
- [x] 1.2 Spend the card's own interior padding, and separate the rows inside a section.
- [x] 1.3 Fill the resting state ring with the card's ground rather than the rail's.

## 2. Verify and publish

- [x] 2.1 Assert the star's reserve as a rule about which line carries it rather than as one width, and run the rail, shell, tree and switcher tests plus both typechecks, lint and the fork check.
- [x] 2.2 Add one `CHANGELOG.md` line, commit only this change's paths, push, archive, and redeploy.
